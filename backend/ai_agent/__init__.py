"""Graph question-answering agent: plan once, run layer specialists in parallel, answer once.

See `docs/AI_AGENT_HANDOFF.md` for the design. The previous agent in `langgraph_agent/` is kept for comparison
until this one replaces it.
"""
